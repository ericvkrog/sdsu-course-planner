# In development, Vite proxies /api → http://localhost:8000 (see vite.config.js).
# No env vars needed for local dev.

# For production builds (e.g. Vercel), set:
# VITE_API_BASE_URL=https://your-railway-backend.up.railway.app
